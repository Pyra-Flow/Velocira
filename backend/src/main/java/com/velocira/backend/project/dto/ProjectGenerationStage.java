package com.velocira.backend.project.dto;

/** Short, outcome-focused stages shown in the default project workspace. */
public enum ProjectGenerationStage {
    UNDERSTANDING,
    CREATING,
    FINISHING,
    READY,
    PARTIAL,
    FAILED,
    CANCELLED,
    NEEDS_INPUT
}

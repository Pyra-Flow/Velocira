# Production must provide deployment-specific origins; development defaults are never used here.
velocira.cors.allowed-origins=${CORS_ALLOWED_ORIGINS}
velocira.generation.ai.shared-secret=${AI_SERVICE_SHARED_SECRET}
management.endpoint.health.show-details=never
server.forward-headers-strategy=framework

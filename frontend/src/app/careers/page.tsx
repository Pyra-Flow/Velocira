import PreReleasePage from "@/components/common/PreReleasePage";

export default function CareersPage() {
  return <PreReleasePage title="No roles are published" description="Velocira does not currently advertise open roles or a recruiting process. This route is retained only to avoid implying that the former listings were real vacancies." />;
}

import PreReleasePage from "@/components/common/PreReleasePage";

export default function ContactPage() {
  return <PreReleasePage title="Support channels are not published" description="This pre-release workspace does not yet provide a verified support, partnership, or status-subscription channel. The former simulated contact form has been removed so messages are never presented as sent when no delivery service exists." />;
}

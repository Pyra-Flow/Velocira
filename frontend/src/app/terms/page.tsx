import PreReleasePage from "@/components/common/PreReleasePage";

export default function TermsPage() {
  return <PreReleasePage title="Terms of service not published" description="The former terms described features and guarantees that are not yet delivered. Publish legally reviewed terms only when the production service, billing, data processing, and support commitments are defined." />;
}
